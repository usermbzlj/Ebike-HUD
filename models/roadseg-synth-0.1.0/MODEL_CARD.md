# roadseg-synth-0.1.0

Dual-scale classmap sidecar trained on **synthetic** simulator GT (ML-005). Not a production road-segmentation net and not evaluated on PKC110 Camera2 1080p60.
Android HybridEngine keeps heuristic instances; classmap supplies road/occlusion and extra unknown_anomaly masks.
